# 2026-09-11 功能验收记录

## 测试范围

- Web：`https://www.lifeyoume.icu/video/zhizhu/`
- 小程序体验版：`智助乖乖`，开发者工具真机模拟页面
- 重点页面：小程序“我的”

## 已通过

| 功能 | 结果 | 证据 |
| --- | --- | --- |
| Web 商品内容包生成 | 通过 | `/api/assistant/generate` 返回 201，结果包含标题、摘要、预览条目 |
| Web 公众号文章助手生成 | 通过 | 切换工具并提交文章主题、读者、角度后返回文章包 |
| Web 朋友圈与社群生成 | 通过 | 切换工具并提交场景、活动、语气后返回内容包 |
| Web 动态价格展示 | 通过 | 页面展示商品 `¥1.00`、文章/社群 `¥0.10` |
| Web 静态资源 | 已修复并通过 | 页面、`zhizhu.css`、图标均 HTTP 200，Console 无错误 |
| 小程序“我的”页面渲染 | 通过 | 用户卡片、统计卡片和四个入口均显示 |
| 小程序支付与订单入口 | 已触发真实支付 | 体验版入口进入微信支付二维码，完成 `¥1.00` 付款 |
| 小程序完成通知页面 | 页面通过 | 页面可打开并展示订阅说明 |
| 小程序原生公众号文章助手 | 通过 | 体验版 `1.0.11` 已上传；模拟器进入 `pages/article/index`，表单和生成按钮正常渲染 |
| 小程序文章预览接口 | 通过 | `POST /api/assistant/generate` 返回 HTTP 201，结果价格为 `0.10` |

## 未通过与原因

### 1. 小程序公众号文章助手 WebView 无法打开（已替换）

现象：

```text
无法打开该页面
不支持打开 https://www.lifeyoume.icu/video/zhizhu/?from=miniapp&tool=article
```

原因：微信小程序的业务域名/`web-view` 合法域名尚未完成配置，代码本身的 URL 使用的是生产 `www` 域名。

处理方式：个人主体小程序不再依赖 `web-view`，改为原生 `pages/article/index`，
直接调用生产文章预览接口；体验版 `1.0.11` 已验证通过。

### 2. 小程序完成通知入口缺少任务编号

当前页面从“我的”直接打开时没有 `jobId`，点击开启通知会提示“缺少任务编号”。通知接口设计要求从具体视频任务页面携带任务编号进入。

后续应将入口改为：

```text
视频任务详情
→ 生成完成通知
→ 携带 jobId 订阅
```

### 3. 微信虚拟支付发货回调未完成

真实付款证据：

- 微信开发者工具出现“微信支付”二维码。
- 开发者微信完成 `¥1.00` 付款。
- 生产 `short-video-studio` 日志收到 `xpay_goods_deliver_notify`。
- 生产订单 `ZZmtwpxnkx6308088c50` 金额为 `100` 分，但当前状态仍为 `PENDING`。

失败原因：

```text
微信发货通知令牌无效
```

已处理：

- 修复并部署 XML 字段空白清理。
- 增加官方 JSON 事件和 `MiniGame.Payload` 订单解析兼容。
- 本地 XML/JSON 回调回归测试通过。

待处理：

- 将微信后台发货推送 Token 与服务器受限环境变量
  `WECHAT_VIRTUAL_NOTIFY_TOKEN` 对齐。
- 在微信后台重新执行发货推送测试，再确认订单状态变为 `DELIVERED`。

### 4. Web 支付真实付款仍未验证

Web 页面已可创建支付宝订单，生产接口和动态价格已验证；支付宝收款由用户确认可用。
微信已完成真单扣款，但自动发货闭环仍待 Token 修正和 `DELIVERED` 证据。
