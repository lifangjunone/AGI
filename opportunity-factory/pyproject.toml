[project]
name = "opportunity-factory"
version = "0.2.0"
description = "Automated opportunity validation and paid agent services"
requires-python = ">=3.11"

[tool.unittest]
start-directory = "tests"
